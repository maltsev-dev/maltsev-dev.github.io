// 404 — no glitch; the page stays silent and stamped.
// This file is kept as a no-op so existing references remain valid.
(function() {
    'use strict';
    // intentionally empty: the stamp-in animation in CSS handles the entrance
})();